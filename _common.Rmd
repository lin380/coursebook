```{r common, include=FALSE}
knitr::opts_chunk$set(
  comment = "#>",
  message = FALSE,
  warning = FALSE,
  collapse = TRUE,
  cache = TRUE,
  out.width = "90%",
  fig.align = 'center',
  # fig.width = 6,
  # fig.asp = 0.618,  # 1 / phi
  fig.show = "hold",
  tidy = TRUE
)

knitr::knit_hooks$set(crop = knitr::hook_pdfcrop)

options(digits = 3)

pacman::p_load(tidyverse, rmarkdown, here, DT, webshot, knitr, tidytext)

theme_set(theme_minimal())

options(knitr.table.format = function() {
  if (knitr::is_latex_output())
    "latex" else "pipe"
})

```

